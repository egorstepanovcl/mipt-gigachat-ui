services:
  gigachat-proxy:
    image: nginx:alpine
    container_name: gigachat-proxy
    ports:
      - "3001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
