# Домашнее задание 3

**Тема:** Основы React: компоненты, JSX и Props

**Время выполнения:** 2 академических часа

**Дедлайн:** 7 дней после открытия задания на платформе

## Цель задания

Создать UI-компоненты будущего приложения в виде статичных React-компонентов с моковыми данными без подключения к API и без реальной бизнес-логики.

## Инструменты

React, TypeScript, CSS Modules / Tailwind / Styled Components.

## Правила приёма работы

1. Прикрепите ссылку на выполненное задание в GitHub.
2. Убедитесь, что доступ к репозиторию открыт.
3. Отправьте работу на проверку.

## Критерии оценки

**Задание считается выполненным, если:**
- прикреплена ссылка на код с выполненным заданием;
- доступ к файлу открыт;
- код выполняется и даёт правильный ответ к задаче.

**Задание считается невыполненным, если:**
- ссылка на код с выполненным заданием не прикреплена;
- отсутствует доступ по ссылке;
- код не выполняется.

## Задание

**Ожидаемый результат:** готова полная оболочка приложения для итогового задания.

> ℹ️ Необязательно создавать все компоненты — некоторых компонентов у вас может не быть. Важно соблюдать требования итогового задания.

### 1. Макет приложения (Layout)

Создайте корневой компонент `AppLayout`, который задаёт общую структуру страницы:

- боковая панель (sidebar) фиксированной ширины слева;
- основная область чата справа (занимает всё оставшееся пространство).

Учитывайте корректное поведение страницы на мобильных устройствах: боковая панель скрывается, появляется кнопка-бургер.

### 2. Sidebar — боковая панель

Компонент `Sidebar` должен содержать:

- кнопку «Новый чат» (с иконкой `+`);
- поле поиска по чатам (`SearchInput`);
- список чатов (`ChatList`) — отобразите минимум **5 моковых чатов**.

Каждый элемент списка `ChatItem` должен включать:
- название чата, обрезанное при переполнении через `text-overflow: ellipsis`;
- дату последнего сообщения;
- кнопки редактирования и удаления (появляются при hover);
- визуальное выделение активного чата.

### 3. Область чата (ChatWindow)

Компонент `ChatWindow` (центральная зона) должен включать:

- заголовок с названием текущего чата и кнопкой настроек;
- область сообщений `MessageList` с вертикальной прокруткой;
- минимум **6 моковых сообщений** (3 — от пользователя, 3 — от ИИ-ассистента) с визуальным разделением:
  - сообщения пользователя — справа, с отличным цветом фона;
  - сообщения ассистента — слева, с аватаром или иконкой GigaChat.

### 4. Компонент сообщения (Message)

Компонент `Message` должен:

- отображать имя или роль отправителя;
- отображать текст с базовой поддержкой markdown (жирный, курсив, списки, блок кода — через `react-markdown`);
- иметь кнопку «Копировать», которая появляется при hover;
- поддерживать два визуальных стиля: `variant="user"` и `variant="assistant"`.

### 5. Индикатор загрузки (TypingIndicator)

Анимированный компонент `TypingIndicator` (три пульсирующие точки) должен отображаться на месте следующего сообщения ИИ-ассистента во время ожидания ответа.

Необходимо показывать его в моковом виде постоянно или по prop `isVisible`.

### 6. Поле ввода (InputArea)

Создайте компонент `InputArea` в нижней части чата со следующими свойствами:

- многострочный `textarea` с автоподстройкой высоты (до 5 строк);
- кнопка «Отправить» активна только при непустом вводе;
- кнопка «Стоп» — заглушена (для будущей функции остановки генерации);
- отправка по `Enter` (перенос строки — `Shift+Enter`) — только обработчик, без реального запроса;
- кнопка прикрепления изображения — только иконка, без логики.

### 7. Модальная панель настроек (SettingsPanel)

Компонент `SettingsPanel` открывается как drawer или modal и должен содержать:

- выбор модели (`<select>` с вариантами: `GigaChat`, `GigaChat-Plus`, `GigaChat-Pro`, `GigaChat-Max`);
- слайдер Temperature (диапазон: 0–2);
- слайдер Top-P (диапазон: 0–1);
- числовое поле Max Tokens;
- `Textarea` для System Prompt;
- переключатель темы (светлая / тёмная) — компонент `Toggle`;
- кнопки «Сохранить» и «Сбросить».

### 8. Форма авторизации (AuthForm)

Создайте отдельный экран `AuthForm`, который отображается вместо `AppLayout`, если пользователь не авторизован. Компонент должен содержать:

- поле для ввода Credentials (Base64-строка, тип `password`);
- радио-кнопки для выбора Scope:
  - `GIGACHAT_API_PERS`
  - `GIGACHAT_API_B2B`
  - `GIGACHAT_API_CORP`
- кнопку «Войти»;
- базовую клиентскую валидацию (поле не должно быть пустым) с выводом ошибки.

### 9. Компоненты ошибок

Создайте следующие компоненты:

- `ErrorMessage` — inline-блок с текстом ошибки и иконкой (например, красный баннер под полем ввода);
- `EmptyState` — заглушка для пустого чата: иллюстрация или иконка + текст «Начните новый диалог».

## Ожидаемая структура файлов

```
src/
├── components/
│   ├── layout/
│   │   └── AppLayout.tsx
│   ├── sidebar/
│   │   ├── Sidebar.tsx
│   │   ├── ChatList.tsx
│   │   ├── ChatItem.tsx
│   │   └── SearchInput.tsx
│   ├── chat/
│   │   ├── ChatWindow.tsx
│   │   ├── MessageList.tsx
│   │   ├── Message.tsx
│   │   ├── TypingIndicator.tsx
│   │   └── InputArea.tsx
│   ├── settings/
│   │   └── SettingsPanel.tsx
│   ├── auth/
│   │   └── AuthForm.tsx
│   └── ui/
│       ├── Button.tsx
│       ├── Toggle.tsx
│       ├── Slider.tsx
│       └── ErrorMessage.tsx
├── styles/
│   └── theme.css (или variables.scss)
└── App.tsx
```

## Технические требования

- Используйте связку **React + TypeScript**.
- Используйте только **моковые данные** — никаких реальных запросов к API.
- Стилизацию выбирайте на своё усмотрение: CSS Modules, Tailwind или Styled Components.
- Настройте тёмную и светлую темы через **CSS-переменные** (`--color-bg`, `--color-text` и т.д.).
- Реализуйте **адаптивность**: корректное отображение от 320px (mobile) до 1440px (desktop).
- Убедитесь, что `react-markdown` подключён и рендерит моковый markdown-текст в компоненте `Message`.

## Чего на этом этапе делать не нужно

- Подключать GigaChat API или любые другие запросы.
- Реализовывать хранение данных (`localStorage`, `IndexedDB`).
- Писать state management (Zustand, Redux).
- Покрывать код тестами.

> Всё это будет сделано на следующих этапах работы над проектом.

## Чек-лист самопроверки

- [ ] Реализованы нужные в вашем проекте компоненты.
- [ ] TypeScript: заданы корректные типы для props каждого компонента.
- [ ] Реализована адаптивная вёрстка (mobile + desktop).
- [ ] Настроены светлая и тёмная темы через CSS-переменные.
- [ ] Реализован markdown-рендеринг в `Message` через `react-markdown`.
- [ ] Соблюдается чистота кода: нет дублирования, компоненты переиспользуемые.
